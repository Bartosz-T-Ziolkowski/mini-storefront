# Mini-Storefront

In order to setup this application, copy its contents to a directory on your computer. Then, run the following commands:
1. npm install
2. npm run dev

Then, open http://localhost:3000 on your browser to view the application contents.